package rendering;

/**
 * An enumeration for potential game object types that would be added into the game if the development was to continue past the module.
 *
 * @author Artur Komoter
 */
public enum ObjectType {
	FLAG
}
